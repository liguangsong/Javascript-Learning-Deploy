describe('practice-1-1', function () {

    it("type of name is string,name is not null type of first character of name is string ", function () {
        expect(typeof(name)).toBe('string');
        expect(name != "").toBe(true);
        expect(typeof (logs[0])).toBe('string');
    });

});